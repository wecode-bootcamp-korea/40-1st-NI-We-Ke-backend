const { appDataSource } = require("./data-source");

const createWishlist = async(userId);

const getWishlistByUserId = async (userId, productOptionid) => {};

module.exports = {
  createWishlist,
  getWishlistByUserId,
};
